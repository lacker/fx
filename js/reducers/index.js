import { combineReducers } from 'redux';

// You don't need to modify this directly.
// Instead, to autogenerate reducers, run `react generate reducer`.
const rootReducer = combineReducers({
});

export default rootReducer;
